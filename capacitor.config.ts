import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kjaras.app',
  appName: 'Kjaras Doña Migue',
  webDir: 'dist/kjaras-pos/browser'
};

export default config;
